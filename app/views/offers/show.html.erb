<div class="container mt-5">

  <div class="row mt-4">
    <div class="col-md-8 offset-md-2">
      <h1 class="display-4 text-center"><%= @offer.title %></h1>
      <div class="banner">
         <%= @offer.image.attached? ? cl_image_tag(@offer.image.key, class: "card-img-top", alt: "Offer Image") : image_tag("default-offer.png", class:"card-img-top", alt: 'Offer image') %>
      </div>
      <hr>
      <div class="card p-4 shadow-sm">
        <h2 class = "text-center pb-3"> What does the service offer?</h2>
        <p class="lead"> 👩🏾‍🤝‍👨🏼 <strong>Number of fans:</strong> <%= @offer.number_of_fan %></p>
        <p class="lead"> 🥳 <strong>Enthusiasm level:</strong> <%= @offer.enthusiasm_level %></p>
        <p class="lead"> 📸 <strong>Activity:</strong> <%= @offer.activity %></p>
        <p class="lead"> 💸 <strong>Price:</strong> <%= number_to_currency(@offer.price, unit: "€") %></p>

        <p class="lead"> 📍 <strong>Localisation:</strong> <%= @offer.localisation %></p>
        <p class="lead"><br><strong>Description:</strong> <%= @offer.description %></p>
        <% if current_user && current_user != @offer.user  %>
          <div class="text-center p-3">
            <div data-controller="toggle" class="mt-1">
              <button data-action="click->toggle#fire" data-toggle-target="togglableButton" class="btn btn-outline-info">Book this performance</button>
                <div data-toggle-target="togglableForm" class="d-none">
                <%= render "bookings/form", booking: @booking %>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <div class="col-12 text-end m-4">
          <%= link_to "Back to offers", offers_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
